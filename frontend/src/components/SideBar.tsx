import { FC } from "react";
import logoWhite from "../assets/logo-white.png";

const SideBar: FC = () => {
  return (
    <>
      <img src={logoWhite} alt="logo of the website" />
    </>
  );
};

export default SideBar;
